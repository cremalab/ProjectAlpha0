<%= form_for(@daily_stage_value) do |f| %>
  <% if @daily_stage_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_stage_value.errors.count, "error") %> prohibited this daily_stage_value from being saved:</h2>

      <ul>
      <% @daily_stage_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
